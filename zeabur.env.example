# Copy these to Zeabur Service -> Variables (when build plan still shows Java)
ZBPACK_PLAN_TYPE=docker
ZBPACK_DOCKERFILE_PATH=Dockerfile

# Root Directory in Zeabur settings must be EMPTY (repo root), NOT "backend"
